<h1>Listing All Artists</h1>

<h3><%= pluralize(@artist.size, 'Artist') %></h3>
<ul>
  <% @artist.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %> : <%=artist.song_count%> song</li>
  <% end %>
</ul>
